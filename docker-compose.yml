services:
  bot:
    build: .
    env_file: .env
    environment:
      MODE: bot
    volumes:
      - death-data:/app/data
      - death-clips:/app/clips
      - ./game_profiles/templates:/app/game_profiles/templates
    restart: unless-stopped

  web:
    build: .
    env_file: .env
    environment:
      MODE: web
      WEB_PORT: "4444"
    ports:
      - "4444:4444"
    volumes:
      - death-data:/app/data
      - death-clips:/app/clips
      - ./game_profiles/templates:/app/game_profiles/templates
    restart: unless-stopped

volumes:
  death-data:
  death-clips:
